import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  page: {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    position: 'relative',
  },
  dateText: {
    width: '100%',
  },
  main: {
    height: '100%',
  },
});

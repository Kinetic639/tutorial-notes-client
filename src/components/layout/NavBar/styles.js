import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    dateText: {
        flexGrow: 1
    },
    centerToolbar: {
        fontsize: '4rem',
        flexGrow: 1
    },
    profile: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    },
    avatar: {
        margin: '0 20px'
    }
}));
